apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: devflow-tls
  namespace: devflow-prod
spec:
  secretName: devflow-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - devflow.example.com
